#include <stdio.h>
#include <string.h>
#pragma warning(disable : 4996)
#define BUF_SIZE 128

int main(int argc, char * argv[])
{// a = 97, z = 122
	char buf[BUF_SIZE];
	int arr[26];

	scanf("%s", buf);
}